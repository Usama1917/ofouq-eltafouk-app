# Developments — خارطة طريق الإصدار الثاني (v2)

> **الوضع:** النسخة الحالية على GitHub (commit `002fea6`) هي **الإصدار الأول (v1)** اللي هينزل على المتاجر. نسخة باك‑أب منها على `/Volumes/Eltafouk2/Backup/ofouq-eltafouk-app_v1_2026-06-30`.
> الملف ده بيوصّف كل التطويرات اللي هتدخل في **v2**، مرتّبة **من الأهم للأقل أهمية**، وكل فكرة معاها خطوات تنفيذ دقيقة.

> ### 🎞️ قاعدة الحركة (مبدأ عام ثابت في كل التطبيق — طلب المالك)
> **أي تغيير من حالة لحالة لازم يبقى بانتقال ناعم — مفيش قفزات مفاجئة أبدًا.**
> يعني أي حاجة بتتغيّر في الواجهة (الانتقال بين الشاشات، الإظهار/الإخفاء، الفتح/القفل للكروت، الضغط على الأزرار، التحميل ← ظهور المحتوى، تبديل التابات/القوائم، تغيّر الأرقام أو التخطيط) بتتعمل بانتقال متحرّك خفيف وناعم (تلاشي/سبرنـج/حركة تخطيط). يبقى سريع وبسيط مش مبهرج. **دايمًا** نحترم إعداد «تقليل الحركة» في النظام فيرجع فوري للناس اللي طالبين حركة أقل. عند بناء أو تعديل أي واجهة → نحرّك الانتقال بشكل افتراضي، مننزّلش تغييرات فورية جافة.

## استراتيجية العمل (مهم)
- نسيب `main` / النسخة المرفوعة = **v1 (الإصدار المنشور)** ثابتة.
- نفتح فرع جديد **`v2-developments`** ونشتغل كل اللي تحت عليه، ونعمل merge أول بأول.
- **اكتشاف مهم:** قاعدة البيانات فيها بالفعل جداول نص‑مبنية بتوفّر شغل كبير:
  - `points_account` + `points_transactions` (نقاط) — **بس من غير `user_id`** (نسخة ديمو، محتاجة تربط بالمستخدم).
  - `games` + `questions` (أسئلة اختيار من متعدد) — موجودة بس مش مربوطة بالدروس.
  - `rewards` + `redemptions` (متجر مكافآت) — موجودة بس من غير `user_id`.
  - `books` + `book_reservations` + `book_purchases` + `book_vouchers` (متجر كتب) — أساس كويس للمتجر.
- كل feature هنمشي فيها نفس الترتيب: **داتابيز → API → لوحة تحكم الأدمن → الموبايل**، وبعدها `pnpm run typecheck` + اختبار محلي.

## مفتاح القراءة
- **الجهد:** 🟢 صغير (أيام) · 🟡 متوسط (أسبوع±) · 🔴 كبير (أسابيع).
- **الأولوية:** الأرقام من الأعلى = الأهم. الترتيب مبني على الأثر على المستخدم + وجود scaffolding + تحليل المنافسين.

---

## ملخّص الأولويات

| # | الميزة | الأثر | الجهد | scaffolding موجود؟ |
|---|--------|------|------|------|
| 1 | نظام التحفيز (نقاط + Streak + هدف يومي + لوحة متصدّرين) | 🔥🔥🔥 احتفاظ يومي | 🟡 | جزئي (points) |
| 2 | الأسئلة والاختبارات بعد الدروس (بنك أسئلة) | 🔥🔥🔥 قيمة تعليمية | 🟡🔴 | جزئي (games/questions) |
| 3 | التخصيص + "كمّل من حيث وقفت" | 🔥🔥 رجوع للتطبيق | 🟢 | البيانات موجودة |
| 4 | مكاسب سريعة (حفظ/ملاحظات/بحث/لوحة تقدّم/تذكيرات) | 🔥🔥 | 🟢🟡 | جزئي |
| 5 | متجر الكتب (نموذج أمازون: سلة/طلبات/تتبّع شحن) | 💰 إيراد | 🔴 | جزئي (books) |
| 6 | تحميل الدروس أوفلاين | 🔥🔥 (مصر) | 🔴 + بنية تحتية | لأ |
| 7 | مساعد AI للمذاكرة | 🔥 | 🟡 | موجود (معطّل) |
| 8 | Backlog: مذاكرة جماعية / حصص لايف / ترجمة ونسخ نصي | 🔥 | 🔴 | لأ |

---

## 1) نظام التحفيز — نقاط + Streak + هدف يومي + لوحة متصدّرين  ✅ خلصت (2026-07)
**الهدف:** نخلّي الطالب يفتح التطبيق **كل يوم**.
**ليه:** Duolingo خفّض الهجر من ٤٧٪ لـ ٢٨٪ بالـ streak بس؛ نون رفعت التفاعل ٤٦–١٩٢٪ بالتحفيز. وده **أرخص وأسرع** لأن النص مبني.
**بيبني على:** `points_account`, `points_transactions` (lib/db/src/schema/points.ts)، `users.points`، `lib/pushNotifications.ts`.

**خطوات التنفيذ:**
1. **داتابيز:**
   - أضف `user_id` لـ `points_account` و`points_transactions` (FK لـ users، فهرس) — تحويلها لكل مستخدم بدل الديمو.
   - جدول جديد `daily_activity` (user_id, date, watched_seconds, lessons_done, points_earned, goal_met) — لحساب الـ streak والهدف اليومي.
   - أعمدة على `users`: `streak_count`, `streak_best`, `last_active_day`, `daily_goal_minutes` (default 20).
2. **API:**
   - دالة مركزية `awardPoints(userId, type, amount, description)` تكتب transaction + تحدّث الرصيد (idempotent لكل حدث).
   - مكاسب النقاط: إكمال درس، اجتياز اختبار، يوم نشاط، إكمال هدف يومي.
   - منطق الـ streak عند `/auth/activity` أو إكمال درس: لو `last_active_day` = إمبارح → +1، لو فجوة → reset، نفس اليوم → بدون تغيير.
   - endpoints: `GET /me/gamification` (رصيد/streak/هدف اليوم/تقدّمه)، `GET /leaderboard?scope=grade|subject&period=week`.
3. **لوحة الأدمن:** صفحة "قواعد النقاط" (قيمة كل حدث)، وعرض رصيد/streak أي طالب في صفحة المستخدم.
4. **الموبايل:**
   - شارة streak 🔥 + الرصيد في هيدر الرئيسية.
   - حلقة "الهدف اليومي" (دائرة تقدّم) في الرئيسية.
   - شاشة "المتصدّرون" (تبويب أو داخل الرئيسية) — ترتيب أسبوعي لنفس الصف.
   - أنيميشن "+10 نقطة" عند إكمال درس/اختبار، وإشعار يومي بالـ streak (push موجود).
**Done:** الطالب يكسب نقاط فعليًا، يشوف streak يزيد يوم بعد يوم، ويظهر في ترتيب صفّه.

---

## 2) الأسئلة والاختبارات بعد الدروس (بنك أسئلة)  ✅ خلصت + اتقفلت (2026-07-05) — التفاصيل في Developments_Done.md
**الهدف:** نحوّل التطبيق من "مشاهدة" لـ "مذاكرة وتقييم" — ده اللي بيبيعه المنافسين.
**ليه:** [أبواب](https://abwaab.com/en) قايمة على "أكبر بنك أسئلة" + تصحيح فوري؛ حتى منصّات الوزارة فيها بنوك أسئلة. من غيره إحنا فيديو وبس.
**بيبني على:** `games` + `questions` (jsonb options + correctIndex) — نوسّعها ونربطها بالدروس.

**خطوات التنفيذ:**
1. **داتابيز:**
   - أضف `lesson_id` (وربما `unit_id`) لـ `questions` عشان كل سؤال يبقى تابع لدرس، + `explanation` (شرح الإجابة) + `difficulty`.
   - جدول `quiz_attempts` (user_id, lesson_id, score, total, started_at, finished_at) + `quiz_answers` (attempt_id, question_id, chosen_index, is_correct).
2. **API:**
   - `GET /lessons/:id/quiz` (أسئلة الدرس بدون كشف الإجابة الصحيحة).
   - `POST /lessons/:id/quiz/submit` (يستقبل الإجابات → يحسب النتيجة → يخزّن attempt → يمنح نقاط عبر `awardPoints`).
   - `GET /me/quiz-history`.
3. **لوحة الأدمن:** محرّر أسئلة لكل درس (إضافة سؤال + ٢–٥ اختيارات + تحديد الصح + شرح)، واستيراد جماعي اختياري (Excel — زي ما فيه import للسيجمنتس).
4. **الموبايل:**
   - زر "ابدأ الاختبار" بعد الفيديو في شاشة الدرس.
   - شاشة اختبار: سؤال‑سؤال → تصحيح فوري + شرح → شاشة نتيجة + نقاط مكتسبة + "أعد المحاولة".
   - وضع "مراجعة" (كل أسئلة الوحدة) لاحقًا.
**Done:** بعد أي درس فيه أسئلة، الطالب يحلّها، ياخد نتيجة وشرح، وتتسجّل وتديله نقاط.

---

## 3) التخصيص + "كمّل من حيث وقفت"  ✅ خلصت + اتقفلت (2026-07-05) — التفاصيل في Developments_Done.md
**الهدف:** رئيسية شخصية تخلّي الطالب يكمّل بسرعة.
**ليه:** **بنجمع بيانات الأونبوردنج (اهتمامات/هدف/مستوى/طريقة تعلّم/وقت مذاكرة) وبنسيبها** — مكسب سريع جدًا من داتا موجودة + متابعة مشاهدة موجودة.
**بيبني على:** `/api/student/onboarding` + حقول users، `lib/watchHistory.ts` + `lesson_watch_progress`.

**خطوات التنفيذ:**
1. **API:** `GET /me/home-feed` يرجّع: آخر درس غير مكتمل (resume)، دروس مقترحة حسب المواد المختارة، "الجديد" في موادك.
2. **الموبايل (الرئيسية):**
   - كارت كبير بارز **"كمّل درسك"** (thumbnail + شريط تقدّم + يفتح على نفس الثانية — `initialSeekSeconds` موجود في المشغّل).
   - قسم **"مقترح ليك"** + قسم **"أُضيف حديثًا"** في موادك.
3. **(لاحقًا)** استخدام "وقت المذاكرة المفضّل" لجدولة تذكيرة (مرتبط ببند ٤).
**Done:** أول ما الطالب يفتح، يلاقي درسه اللي وقف فيه ومقترحات تخصّه.

---

## 4) باقة المكاسب السريعة  ✅ خلصت (2026-07-05) + تحسينات (2026-07-06) — التفاصيل في Developments_Done.md
> **تحسينات 2026-07-06:** زرار ملاحظة جوّه المشغّل (ضغطة سريعة/مطوّلة بأنيميشن FLIP) + كروت الدرس القابلة للطي + بحث fuzzy (pg_trgm) + عدّ مشاهدة حقيقي + تظبيطات واجهة. **لوحة التقدّم (٤.د) اتشالت** بقرار المالك (متكرّرة). للديبلوي: `CREATE EXTENSION pg_trgm;`.

حزمة صغيرة عالية الأثر، كل بند مستقل:

**4.أ — حفظ الدروس (مفضّلة) ⭐ 🟢**
- داتابيز: `lesson_bookmarks` (user_id, lesson_id, created_at).
- API: `POST/DELETE /lessons/:id/bookmark`، `GET /me/bookmarks`.
- الموبايل: زر نجمة في شاشة الدرس + قائمة "المحفوظة".

**4.ب — ملاحظات بتوقيت الفيديو 📝 🟡**
- داتابيز: `lesson_notes` (user_id, lesson_id, at_seconds, body, created_at).
- API: CRUD بسيط.
- الموبايل: زر "ملاحظة" في المشغّل ياخد الثانية الحالية؛ الضغط على الملاحظة يقفز لتوقيتها.

**4.ج — بحث عن درس 🔎 🟢**
- API: `GET /search?q=` (مواد/وحدات/دروس بالعربي والإنجليزي).
- الموبايل: شريط بحث في الرئيسية/الفيديوهات.

**4.د — لوحة تقدّم الطالب 📊 🟢**
- نعرض داتا `watch-history` الموجودة كرسوم تحفيزية: "أنجزت X٪ من الفيزياء"، إجمالي ساعات المذاكرة، أيام الـ streak.
- مكان: تبويب/قسم في الإعدادات أو الرئيسية.

**4.هـ — تذكيرات مذاكرة + هدف يومي ⏰ 🟢**
- نستخدم push (موجود) + "وقت المذاكرة المفضّل" من الأونبوردنج لجدولة تذكيرة يومية: "وقت مذاكرة الفيزياء 💪".
**Done لكل بند:** يشتغل end‑to‑end ويتفحص typecheck.

---

## 5) متجر الكتب — نموذج أمازون (سلة + طلبات + تتبّع شحن)  🔴  💰
**الهدف:** تحويل قسم الكتب لمتجر حقيقي: منتجات → سلة → دفع → طلب → **تتبّع شحنة**.
**ملاحظة:** ده **مسار قيمة منفصل (تجارة)** عن مميزات التعلّم، فممكن يمشي بالتوازي. أساسه نص‑مبني.
**بيبني على:** `books`, `book_reservations`, `book_purchases`, `book_vouchers` (فيها سعر بالجنيه، شحن، خصومات، أكواد).

> **🔄 الحالة (2026-07-06): قيد التنفيذ — الأساس كامل ومتكوميت محليًا** (`d16541d` على `v2-developments`؛ **مش مدفوع/مش متطبّق على الإنتاج**). خلص: الداتابيز (جداول المتجر) + السيرفر (`routes/store.ts` + `routes/shipping.ts`) + لوحة الأدمن (تبويب **«المنتجات والشحن»** بـ٣ تابات + تبويب **«الطلبات»**) + شاشات الموبايل (تبويب **«المتجر»**: كتالوج→كتاب→سلة→دفع COD→تتبّع ٦ مراحل→طلباتي→مفضّلة) + كوبونات (نسبة/مبلغ/شحن مجاني) + مراجعة أمنية عدائية وإصلاحاتها. **الباقي:** تعديلات المالك الكتير + بوابة دفع Paymob (تفعيل الوصول الرقمي من webhook مؤكّد بس) + أسعار الشحن الحقيقية من عقد البريد + كتب حقيقية + فلترة بالصف + الرفع (GitHub + `db push`). **التفاصيل الكاملة ونقطة الاستئناف والـ To-Do في [`Developments_Done.md`](./Developments_Done.md) — قسم الفيز ٥.**

**خطوات التنفيذ:**
1. **داتابيز (توسعة):**
   - `books`: أضف `stock`, `images[]` (jsonb), `weight` اختياري. (موجود: سعر/خصم/شحن مجاني/فئة.)
   - `cart_items` (user_id, book_id, qty).
   - `shipping_addresses` (user_id, name, phone, governorate, city, street, notes, is_default).
   - `orders` (user_id, address snapshot, subtotal, shipping, discount, total, voucher_code, payment_method ["cod"|...], status, created_at).
   - `order_items` (order_id, book_id, title snapshot, unit_price, qty).
   - `order_status_history` (order_id, status, note, created_at) — لتتبّع الشحنة.
   - حالات الطلب: `placed → confirmed → packed → shipped → out_for_delivery → delivered` (+ `cancelled`).
2. **API (الطالب):** `GET /store/books`, `GET /store/books/:id`, سلة (`GET/POST/PATCH/DELETE /store/cart`)، عناوين (CRUD)، تطبيق كوبون، `POST /store/checkout` (يعمل order + يفرّغ السلة + يخصم مخزون + يطبّق فاوتشر)، `GET /store/orders`, `GET /store/orders/:id` (مع timeline الحالات).
3. **لوحة الأدمن:** إدارة المنتجات (CRUD + صور + مخزون + خصومات)، إدارة الطلبات (قائمة + تفاصيل + **تغيير الحالة** اللي بيغذّي تتبّع الطالب)، الكوبونات (موجود `book_vouchers`).
4. **الموبايل (إعادة بناء tab/books.tsx):** واجهة متجر (شبكة منتجات + بحث/فئات) → صفحة منتج → سلة → اختيار عنوان → ملخّص الطلب + كوبون → تأكيد (الدفع عند الاستلام كبداية) → **شاشة تتبّع الطلب** (timeline حالات + رقم طلب) + "طلباتي".
**Done:** الطالب يضيف كتب للسلة، يطلب بعنوان، ويتابع حالة الشحنة؛ والأدمن يدير المنتجات والطلبات ويحدّث الحالة.
**ملاحظات:** نبدأ بالدفع **عند الاستلام (COD)**؛ بوابة دفع إلكترونية مرحلة تالية. وحساب الشحن حسب المحافظة.

---

## 6) تحميل الدروس أوفلاين  🔴 + بنية تحتية
**الهدف:** الطالب يحمّل الدرس ويتفرّج بدون نت (مهم جدًا في مصر).
**عائق أساسي:** الفيديوهات حاليًا **على يوتيوب** وسياسته **تمنع التحميل** — لازم نستضيف الفيديوهات بنفسنا الأول (MP4 + تشفير/توقيع روابط). المشغّل بيدعم MP4 بالفعل.
**خطوات مختصرة:** اختيار استضافة فيديو (Bunny/Cloudflare Stream/مخزن خاص) → رفع وربط بالدروس → تحميل محلي مشفّر بـ `expo-file-system` → إدارة المساحة + صلاحية (يرتبط بالاشتراك) → قائمة "تنزيلاتي".
**قرار مطلوب من المالك:** ميزانية الاستضافة + نقل الفيديوهات من يوتيوب.

---

## 7) مساعد AI للمذاكرة  🟡
**الهدف:** "اسأل أي سؤال في الدرس" — مساعد ذكي.
**بيبني على:** `routes/openai.ts` (موجود، مؤمّن، محدود المعدّل) + شاشة `chat.tsx` (معطّلة في الإطلاق الناعم).
**خطوات:** تفعيل بعد v1 → ربط المحادثة بسياق الدرس/المادة → ضوابط استخدام/تكلفة → إظهار التبويب (`SOFT_LAUNCH_MODE`).

---

## 8) Backlog (لاحقًا — أثر عالي وجهد كبير)
- **مذاكرة جماعية/مجتمع** (نموذج نون: غرف مذاكرة + مسابقات حية) — محتاج إشراف وبنية real‑time.
- **حصص لايف** — الأونبوردنج مظهّر طلب عليها؛ بنية تقيلة (WebRTC/بث).
- **ترجمة ونسخ نصي للفيديو + بحث داخل الفيديو** (transcript search).
- **Picture‑in‑Picture + ترجمات (CC)** في المشغّل.
- **نظام دعوة/إحالة (Referral)** لزيادة الانتشار.

---

## خطة البداية المقترحة (من بكرا)
**نبدأ بالبند ١ (التحفيز)** لأنه: نص‑مبني (أرخص/أسرع)، وبيزوّد الاستخدام اليومي فورًا. الترتيب العملي:
1) ربط النقاط بالمستخدم + منطق الـ streak والهدف اليومي (داتابيز + API).
2) عرضهم في الرئيسية (شارة streak + حلقة الهدف + رصيد).
3) لوحة المتصدّرين.
ثم ننتقل للبند ٢ (الاختبارات). متجر الكتب (بند ٥) ممكن يمشي كمسار مستقل بالتوازي.

> **المصادر (تحليل المنافسين):** أبواب · نون أكاديمي · نجوى · منصّات الوزارة (مدرستنا/حصص مصر) · Duolingo (gamification) · اتجاهات تصميم تطبيقات التعليم 2025.
