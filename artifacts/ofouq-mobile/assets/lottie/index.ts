// The owner's exact LottieFiles flame, loaded as an opaque object for LottieBox.
// Loaded via require so tsc doesn't infer a giant literal type from the JSON.
export const iconFire = require("./icon-fire.json") as object; // streak icon (Fire.json)
