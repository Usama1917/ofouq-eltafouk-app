// review F-29: this was an orphaned older copy of the year-list. The live screen
// is videos/index.tsx and the academic tab is href:null. To avoid divergence while
// keeping the expo-router route alive, re-export the videos screen as the default.
export { default } from "../videos";
