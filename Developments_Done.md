# Developments_Done — اللي خلص فعليًا من خطة v2

> هذا الملف **سجلّ المُنجَز**: كل بند خلص من [`Developments.md`](./Developments.md) يتسجّل هنا —
> إيه اللي اتعمل من اللي اتفقنا عليه، إيه اللي في الخطة، وإيه اللي **انضاف** بخلاف الخطة.
> القاعدة: لما ننتقل لفيز جديدة، نسجّل الفيز اللي خلصت هنا.
> كل الشغل ده على فرع `v2-developments` (نسخة المتاجر v1 على `main` متلمستش). الفيز ١ **متكوميت** (`62c0f3c` التحفيز + جزء في `75abe32`).

## حالة الأولويات (من Developments.md)

| # | الفيز | الحالة |
|---|-------|--------|
| 1 | نظام التحفيز (نقاط + Streak + هدف يومي + متصدّرين) | ✅ **خلصت** + إضافات (متكوميت) |
| 2 | الأسئلة والاختبارات بعد الدروس | ✅ **خلصت + اتقفلت** (2026-07-05) — اختبارات دروس + قفل مشاهدة + امتحانات فصل + مستوى الطالب + إشعارات امتحانات (متكوميت + مدفوعة) |
| 3 | التخصيص + "كمّل من حيث وقفت" | ✅ **خلصت + اتقفلت** (2026-07-05) — الرئيسية الشخصية: كارت «كمّل درسك» + «مقترح ليك» |
| 4 | باقة المكاسب السريعة | ✅ **خلصت** (2026-07-05) — حفظ + ملاحظات بتوقيت + بحث + لوحة تقدّم + تذكيرة مذاكرة |
| 5 | متجر الكتب (نموذج أمازون) | ⬜ لسه |
| 6 | تحميل الدروس أوفلاين | ⬜ لسه |
| 7 | مساعد AI للمذاكرة | ⬜ لسه |
| 8 | Backlog | ⬜ لسه |

---

## ✅ الفيز ١ — نظام التحفيز (مكتملة + متجرّبة)

### اللي اتفقنا عليه واتعمل (من الخطة)
- **قاعدة البيانات:** ربط النقاط بكل طالب (`points_account`/`points_transactions` بقى فيهم `user_id` + `source_key` لمنع التكرار)، جدول `daily_activity` (نشاط يومي للستريك والهدف)، وأعمدة على `users`: `streak_count`, `streak_best`, `last_active_day`, `daily_goal_minutes` (افتراضي **٣٠ دقيقة**).
- **منطق الخادم** (`api-server/src/lib/gamification.ts`): `awardPoints` (idempotent)، `recordLessonActivity` (نقاط + ستريك + هدف يومي) مربوط بمشاهدة الدرس (إكمال عند **٩٠٪**)، حساب اليوم بتوقيت **القاهرة**.
- **روابط الطالب:** `GET /me/gamification` (الرصيد/الستريك/تقدّم اليوم)، `GET /leaderboard`.
- **لوحة الأدمن:** كارت نقاط وستريك الطالب في صفحة تفاصيله.
- **الموبايل:** شريط التحفيز في الرئيسية (🔥 ستريك + 🎟️ نقاط + حلقة الهدف)، صفحة المتصدّرين، أنيميشن "+نقاط" عند إكمال الدرس.

### القرارات النهائية (قيم النقاط والمنطق)
- إكمال درس **+١٠** · أول نشاط في اليوم **+٥** · تحقيق الهدف اليومي (٣٠ دقيقة) **+٢٠**.
- **مكافآت السلسلة:** يوم ١٠ → **+٢٠** · يوم ٢٠ → **+٥٠** · يوم ٣٠ → **+١٠٠** · من يوم ٣١ وطالع **+٥/يوم**.
- شرط الستريك = **أي نشاط** (يفتح أي درس النهارده).
- المتصدّرون = **ترتيب إجمالي (تراكمي)**، نفس **الصف الدراسي**، **الاسم الأول + الصورة**.
- **أي حد نقاطه صفر مايظهرش** في أي ترتيب/تصنيف.
- النقاط **عرض وتحفيز بس** (مفيش صرف لسه).

### إضافات بخلاف الخطة الأصلية (اتطلبت أثناء التنفيذ)
- **صفحة "النقاط والستريك"** في الإعدادات (بين الاشتراكات وتواصل معنا، للطلاب بس): ملخص ثابت (نقاط/ستريك/**التصنيف الحالي**) + **سجل كامل** لكل نقطة (من إيه/إمتى/كام) بيعمل سكرول لوحده + زر **"i"** بيكبر لكارت **قواعد النقاط** بكل الشروط وقيمها.
- `GET /me/points-history` (سجل النقاط).
- أيقونة النقاط بقت **تذكرة 🎟️** في كل الأب.
- **الصف الدراسي** للقراءة فقط في الإعدادات (التعديل عبر الدعم)؛ إجباري في الأونبوردنج (موجود أصلاً).
- التحفيز كله **مربوط بدور "طالب"** (المعلم مايشوفش نقاط/ستريك).

### نظام الرسائل المؤتمتة + تقرير الإرسال (مسار إضافي كامل)
- جداول: `automated_messages` (إعدادات الرسائل)، `notification_campaigns` + عمود `campaign_id` على `notifications` (للتقرير).
- **٣ رسائل مؤتمتة** قابلة للتعديل من الأدمن (نص/ميعاد/تشغيل): **تذكير مسائي 🔥** (عامل يومي مجدوَل) + **تهنئة الهدف 🎉** + **وسام النقاط 🏅** (حدثيّة).
- صفحة "إرسال الإشعارات" بقت **٣ أقسام**: إرسال يدوي · الرسائل المؤتمتة · **تقرير الإرسال** (مين وصله إيه/إمتى/مين فتح، بالرسالة وبالطالب). الإرسال اليدوي بيتسجّل كمان.

### تطوير خريطة الأونر (مسار إضافي)
- الـ endpoint الجغرافي زوّد لكل محافظة: **توزيع الصفوف** (أول/تاني/تالت ثانوي) + **المشاهدات بالساعة**.
- الكارت العائم: زر **⚙️ إعدادات** (٨ عناصر بسويتش إظهار/إخفاء، بحد أقصى ٤) + زر **توسيع** بأنيميشن تحوّل (morph) لكارت كبير بنصّين (رسمة المحافظة بزوم + كل البيانات) + زر توسيع في كل صف بالقائمة.
- **Option + سكرول = زوم** ناحية المؤشر · الكارت مبقاش بيتقطع · حواف التظليل دائرية · تباين الكروت اتظبط في الفاتح والداكن.

### إصلاحات RTL/واجهة
- محاذاة الأونبوردنج (عنوان السؤال + الإجابات) اتظبطت من الجذور (`direction:"ltr"` + flex).
- عناصر البار الجانبي للأونر بقت واضحة في الوضعين.

---

## ✅ الفيز ٢ — الأسئلة والاختبارات بعد الدروس (بدأت 2026-07-01 · **اتقفلت 2026-07-05**)
اتفقنا نقفل الفيز ١ وننتقل للفيز ٢. التنفيذ هيمشي بنفس الترتيب: **داتابيز → API → لوحة الأدمن → الموبايل** + `typecheck` واختبار محلي.

> **🔒 الفيز ٢ اتقفلت رسميًا في 2026-07-05.** كل اللي تحت (اختبارات الدروس + قفل المشاهدة + امتحانات الفصل بالتحكم المستقل + مستوى الطالب + إشعارات الامتحانات المؤتمتة) **خلص واتجرّب واتكوميت واترفع** على `origin/v2-developments`. البنود المؤجّلة (تحت في قسم «مؤجّل / لسه») **اتنقلت للـ Backlog** — مش شرط للفيز، وممكن نرجعلها بعدين. **الخطوة الجاية: الفيز ٣ (التخصيص + «كمّل من حيث وقفت»).**

### القرارات المتفق عليها مع المالك (2026-07-01) — كاملة
> اتحدّدت كلها بالأسئلة (٢٠ سؤال على ٥ مجموعات) قبل أي تعديل كود.

**المكان والصلاحية:**
- الاختبار في تبويب **«الدروس المرئية» بس** — يتربط بجدول **الفيديوهات** (كل «درس مرئي» = فيديو؛ «تقسيمات الدرس» = مقاطع الفيديو).
- **للمشتركين بس** (زي الفيديو؛ الأدمن/الأونر مستثنيين).
- درس **من غير أسئلة → الكارت يختفي خالص**.

**كارت الاختبار (شاشة الدرس):**
- كارت **بسيط ونظيف** يظهر **تحت وصف الدرس مباشرة وقبل «تقسيمات الدرس»**؛ الضغط عليه يدخّل الامتحان.
- **بعد الحل:** الكارت يعرض ملخّص **آخر محاولة** (النسبة + النقاط) + زر **«أعد المحاولة»** + رابط **«كل المحاولات»**.
- الطالب يقدر يشوف **كل محاولاته السابقة** (سجل كامل).

**النقاط والقواعد:**
- قيمة الاختبار الكامل (١٠٠٪) = **١٠ نقاط**، وتُحتسب **بالنسبة المئوية** للنتيجة (٨٠٪ = ٨).
- النقاط **أول محاولة بس** (idempotent) — الإعادة للمراجعة من غير نقاط جديدة.
- حل الاختبار **يحسب نشاط يومي يحافظ على الستريك** (متسق مع قاعدة «أي نشاط»).
- **مجرد نسبة** (مفيش ناجح/راسب)، والاختبار **اختياري**، **من غير تايمر**.

**الأسئلة ومحرّر الأدمن:**
- **اختيار من متعدد، ثابت ٤ اختيارات** (أ ب ج د) + تحديد الصح.
- **الشرح اختياري** لكل سؤال.
- الأسئلة **عربي + إنجليزي** (حقل لكل لغة، والإنجليزي يقع على العربي لو فاضي).
- **صورة/رسمة مع السؤال مدعومة من دلوقتي**.
- حقل **الصعوبة (سهل/متوسط/صعب) للأدمن بس** (الطالب مايشوفهوش).
- **بنك أسئلة لكل درس** + الأدمن **يحدد عدد الأسئلة اللي تتعرض** لكل درس، والطالب ياخد **عيّنة عشوائية** بالعدد ده (لو البنك أصغر → يعرض كل الموجود).
- **استيراد Excel للأسئلة من دلوقتي** (زي استيراد السيجمنتس).

**تجربة الامتحان:**
- **سؤال‑سؤال** + **شريط تقدّم «سؤال ٣ من ١٠»** + الطالب **يرجع ويعدّل** قبل التسليم.
- **التصحيح في الآخر** (زي الامتحان الحقيقي).

**شاشة النتيجة:**
- **نتيجة + مراجعة كاملة** (النسبة + النقاط + كل سؤال بإجابتك والصح والشرح) + زرّي «أعد المحاولة» و«رجوع للدرس».
- **رسالة تشجيع متدرجة** (ممتاز ٨٥٪+ / كويس ٦٠٪+ / راجع تاني أقل).
- **أنيميشن «+نقاط»** بعد التسليم (زي إكمال الدرس، أول محاولة بس).
- **«أعد المحاولة»** يسحب عيّنة عشوائية جديدة ويبعثر الاختيارات.

**مؤجّل للاحقًا:** إحصائيات الأدمن للاختبار (عدد الحلّالين/متوسط النتيجة/أصعب سؤال).

### خطة التنفيذ (بالترتيب)
1. **داتابيز (إضافي وآمن):** جدول أسئلة الفيديو (`video_questions`) + محاولات (`quiz_attempts`) + إجابات المحاولة + عمود «عدد الأسئلة المعروضة» على الفيديوهات → `db push`.
2. **API:** جلب الاختبار (عيّنة عشوائية بدون كشف الصح، محمي بالاشتراك) + تسليم (تصحيح + تخزين + نقاط أول محاولة + نشاط الستريك) + سجل المحاولات + مسارات الأدمن (CRUD أسئلة + رفع صورة + استيراد Excel).
3. **لوحة الأدمن:** محرّر أسئلة لكل فيديو + استيراد Excel.
4. **الموبايل:** كارت الاختبار + شاشة الامتحان + شاشة النتيجة، عربي/إنجليزي و RTL.
5. `pnpm run typecheck` + اختبار محلي بعد كل طبقة.

> ملاحظة: طبقات ١→٥ دي (اختبار الدرس نفسه) كانت متعملة من جلسة سابقة كملفات غير متكوميت؛ اتكوميت في جلسة **2026-07-04** ضمن الدفعة تحت.

---

## 🚀 جلسة 2026-07-04/05 — دفعة كبيرة (متكوميت + مدفوعة على GitHub)

فرع `v2-developments`. الكوميتات: **`107bfe5`** (الدفعة الكبيرة) + **`37880ed`** (تصحيح فوري + مستوى الأدمن). **مدفوعة على GitHub** (`origin/v2-developments`). ملفات `artifacts/api-server/uploads/` اتضافت لـ `.gitignore` (صور/thumbnails مولّدة runtime، مش سورس).

### أ) قفل الاختبار على المشاهدة الحقيقية (Watch-Gate) + مشاهدة حقيقية بدل الشريط
**المشكلة اللي اتحلّت:** كل حاجة كانت معتمدة على **مكان شريط التقدّم** (`currentSeconds`) — الطالب يقدر يسحب للآخر ويتسجّل إنه شاهد/يكسب نقاط. دلوقتي في **تغطية حقيقية**.
- **المشغّل** (`ofouq-mobile/components/AcademicVideoPlayer.tsx`): عدّاد تغطية بيحسب الثواني اللي اتشغّلت فعلاً (buckets)، **بيتجاهل السحب/القفز** (فرق أكبر من ٢.٥ث = seek)، ومحفوظ في AsyncStorage لكل فيديو (يتجمّع عبر الجلسات). بيبعت `watchedSeconds` في `onProgressUpdate`.
- **قاعدة البيانات:** عمود `watched_seconds` على `lesson_watch_progress` (مونوتوني، غير `current_seconds` اللي هو مكان الاستكمال) + عمودين على `videos`: `quiz_watch_gate_enabled` + `quiz_watch_gate_percent` (افتراضي off / ٧٥٪).
- **الأدمن (لكل فيديو):** مفتاح "اقفل الاختبار لحد ما يشوف نسبة" + خانة النسبة، في `quiz-manager.tsx` (محرّر الدرس).
- **السيرفر:** القفل مطبّق **server-side** في `routes/quiz.ts` على ٣ مستويات (info + بدء + تسليم). الأدمن/الأونر بيتجاوزوا.
- **الأهم:** **النقاط + الإكمال (٩٠٪) + سجل المشاهدة** كلهم اتحوّلوا للمشاهدة الحقيقية (`academic.ts`: progress handler + watch-history read). السحب مش بيحتسب في أي حاجة. **متأكد بالاختبار:** سحب للآخر = ٣٪ مقفول، مشاهدة ٩١٪ = مكتمل + نقطة.

### ب) امتحانات الفصل (Chapter/Unit Exams) — الفيتشر الكبيرة (كل القرارات مع المالك)
كارت **امتحان الفصل** في آخر كل فصل (وحدة)، تصميم مميّز (بنفسجي، `ChapterExamCard.tsx`)، **مقفول لحد ما الأدمن يفتحه**. جواه **امتحانين منفصلين** (الطالب يختار):

**القرارات المتفق عليها (2026-07-04):**
- الامتحان على مستوى **الفصل/الوحدة** بس دلوقتي (مش المادة).
- فتح الامتحان = **تحكم الأدمن** (publish toggle لكل وحدة).
- **٣ مستويات** للطالب: مبتدئ/متوسط/متقدّم · **موزون بالصعوبة** (صعب×٣ متوسط×٢ سهل×١) · من **أول محاولة** لكل سؤال · الحدود: **<٥٠٪ مبتدئ · ٥٠–٨٠٪ متوسط · >٨٠٪ متقدّم** (تحت ٥ أسئلة = "لسه بيتحدد").
- **تعريف الغلط** = غلط في **أول محاولة** ولسه ماظبطهوش (يتشال من القايمة أول ما يظبطه = mastered).
- توزيع الامتحان المتكيّف حسب المستوى: **متقدّم ٧٠ صعب/٣٠ متوسط · متوسط ٢٥/٥٠/٢٥ · مبتدئ ٦٠ سهل/٣٠ متوسط/١٠ صعب**.
- **امتحان أ (راجع أخطاءك):** أخطاء الطالب في دروس الفصل، **بحد أدنى ٧٥٪** من إجمالي أسئلة الفصل (لو قليلة يكمّل بأسئلة صح بأولوية صعب←متوسط←سهل؛ لو كتير يعدّي ٧٥٪) · **تصحيح فوري + شرح بعد كل سؤال** · بدون مؤقّت · نقاط أول محاولة = نقاط اختبار درس (١٠).
- **امتحان ب (امتحان الفصل المتكيّف):** بنك **مستقل للفصل** يضيفه الأدمن (مصنّف صعب/متوسط/سهل) · عدد الأسئلة المعروضة يحدده الأدمن · **مؤقّت يحدده الأدمن + تسليم تلقائي + النتيجة في الآخر** (رسمي) · نقاط أكبر (افتراضي ٥٠) أول محاولة، ويعيد للتدريب بدون نقاط.
- **مستوى الطالب** بيظهر عند الطالب (شارة جنب كل مادة) **وفي لوحة الأدمن** (قسم "مستوى الطالب في المواد" في تفاصيل الطالب).

**اللي اتبنى (متأكد end-to-end):**
- **قاعدة البيانات** (`lib/db/src/schema/unit-exams.ts` — ٥ جداول): `unit_exams` (إعدادات) · `unit_exam_questions` (بنك المتكيّف) · `unit_exam_attempts` + `unit_exam_attempt_answers` · **`student_question_stats`** (محرّك المستوى/الأخطاء — سطر لكل (طالب،سؤال) فيه `first_attempt_correct` immutable + `ever_correct`، denormalized subjectId/unitId/difficulty).
- **المحرّك** (`api-server/src/lib/exam-engine.ts`): تسجيل الإحصائيات · حساب المستوى (`getStudentLevels`/`getStudentSubjectLevel`) · تجميع أخطاء الفصل (`buildMistakesReview`) · العيّنة المتكيّفة (`pickAdaptiveIds` + `ADAPTIVE_MIX`). التسجيل بيتنده من كل تسليم اختبار (درس + فصل).
- **السيرفر** (`api-server/src/routes/unit-exams.ts`): أدمن (GET/PUT إعدادات+بنك · مستوى الطالب) + طالب (info · review + submit · adaptive + submit). الـ review بيرجّع الإجابة+الشرح (تدريبي، للتصحيح الفوري)؛ الـ adaptive بيخفيهم (رسمي). النقاط عبر `recordExamActivity` في `gamification.ts` (idempotent، أول محاولة). endpoint مستوى الطالب للموبايل: `GET /me/subject-levels`.
- **الموبايل:** شاشة `app/quiz.tsx` اتوسّعت للامتحانات (params: `examKind` = unit-review/unit-adaptive + `unitId`) — نفس الرندر، + مؤقّت تنازلي للمتكيّف + تصحيح فوري (ألوان صح/غلط + شرح) للمراجعة · `ChapterExamCard.tsx` في آخر قايمة دروس الفصل (`academic/lessons.tsx` ListFooter) · شارة المستوى في `academic/subjects.tsx`.
- **الويب:** `chapter-exam-manager.tsx` (بنك بالصعوبة عبر `QuizManager variant="chapter"` + إعدادات) مدمج في محرّر الوحدة (`admin-academic.tsx`) · قسم مستوى الطالب في `admin-panel.tsx`.

### ج) إصلاحات وتحسينات واجهة (نفس الجلسة)
- **الفونت (ويب):** استبدال SF Arabic بنسخة **variable font حقيقية** (أوزان ١٠٠–٩٠٠ من مصدر آبل، محوّلة woff2) — أصلح الـ faux-bold في كل الداشبورد. (`ofouq-eltafouk/src/index.css` + `assets/fonts/SFArabic-VF.*`).
- **العرض الشجري للمحتوى الأكاديمي (ويب):** عرض تاني (شبكي/براكت رأسي) جنب عرض الكروت، السنة بتفتح شجرتها كاملة، خطوط وصل CSS منطقية (RTL صح)، سحب بالماوس (grab-to-pan)، endpoint واحد بيرجّع الشجرة (`academic.ts` `/admin/academic/years/:id/tree`). أيقونة العرض شكل شجرة.
- **مراجعة إجابات الاختبار (موبايل):** اتصلحت المحاذاة RTL بالكامل (بلغة المحتوى + `direction:"ltr"` neutraliser بدل الـ isRTL غير الموثوق) · علامة الصح/الغلط بقت **فوق الصورة على أقصى اليمين** (مش بتحجز عمود) · طبقة تلوين فوق الاختيار الصورة · "السؤال رقم N" على اليمين.
- **نوتيفيكيشن الاستكمال (موبايل):** كان بيعلّق على التحميل — اتصلح بتأجيل الفتح التلقائي للفل-سكرين لحد ما المشغّل يجهز (يوتيوب بيرفض autoplay بدون لمسة). دلوقتي بيفتح الدرس على مكانك وتضغط تشغيل.

### د) جلسة 2026-07-05 (تكملة) — تحكم مستقل للامتحانين + مكان جديد + تسمية + عرض شجري
- **كل امتحان مستقل تمامًا:** بدل مفتاح فتح/قفل واحد للكارت، بقى فيه **مفتاحين منفصلين** — `review_published` + `adaptive_published` + `review_points` (أعمدة جديدة في `unit_exams`، مضافة بأمان + backfill من `is_published` القديم). كل امتحان بيتفتح/يتقفل ويتحسب نقاطه لوحده؛ الكارت في التطبيق بيظهر لو أي امتحان مفتوح، وكل صف حسب حالته (`resolveUnitAccess` بقى بياخد `examType`، والـ info بيحسب `review.available`/`adaptive.available` بالعلم الخاص بكل واحد). E2E: ١٧ اختبار عدّى (فتح واحد وقفل التاني بيشتغل صح في الاتجاهين).
- **مكان التحكم اتنقل:** اتشال من شاشة تعديل الفصل → بقى في **شاشة الدروس**: زرار «إضافة امتحان [الفصل]» جنب «إضافة» (مايشتغلش إلا لو فيه درس واحد على الأقل، والدرس مش لازم يكون فيه أسئلة)، وبعد الإضافة **كارت مميز دايمًا تحت** بيفتح إعدادات كل امتحان لوحده. زرار «إضافة» بيختفي وقت ما ديلوج الامتحان مفتوح. (`admin-academic.tsx` + `chapter-exam-manager.tsx` بقسمين منفصلين).
- **تسمية الامتحانين (تطبيق + داشبورد + سجل النقاط):** «راجع أخطاءك» → **«امتحان الاستدراك»** (وصف: «ركزنا لك على النقط اللي محتاجة محاولة تانية.») · «امتحان الفصل» → **«تحديك الخاص»** (وصف: «أسئلة اختيرت بناءً على مستواك.»). EN: Catch-up exam / Your own challenge.
- **إصلاح اتجاه كروت الامتحان (موبايل):** الصفين كانوا ناقصين `direction:"ltr"` فكانوا بينقلبوا مع forceRTL — دلوقتي (عربي): الأيقونة أقصى اليمين ← الاسم+الوصف ← فراغ ← سهم أقصى الشمال (EN زي ما هو). (`ChapterExamCard.tsx`).
- **العرض الشجري:** (١) endpoint الشجرة بيرجّع حالة امتحان كل فصل، وبيظهر **كارت امتحان جنب الفصل** (الفصل فاضل متمركز تحت خط الوصل + مسافة فاضية على اليمين، والامتحان على الشمال). (٢) **اتشالت الأزرار** من كروت المادة/الفصل/الدرس. (٣) **الضغط على الكارت** بينقل لنفس العنصر في عرض الكروت بين إخواته + **وميض ورعشة خفيفة ثانية** (بيحترم reduced-motion)؛ الضغط على كارت الامتحان بيفتح إعداداته. (`academic.ts` tree + `admin-academic.tsx` + `index.css`).

### هـ) جلسة 2026-07-05 (تكملة) — تبويب «إشعارات الامتحانات» في الرسائل المؤتمتة
تبويب جديد في «الرسائل المؤتمتة» بـ٣ رسائل أوتوماتيك، **كلها بتتبعت للمشتركين في المادة بس** (ذكية):
- **`exam_opened`** — بتتبعت لكل المشتركين في المادة أول ما يتفتح امتحان في فصل (بتشتغل مرة واحدة عند التحول مقفول→مفتوح، مش كل حفظ). لو الاتنين فتحوا مع بعض → {exam}="امتحان الفصل"، لو واحد → اسمه. المتكيّف مايتعلنش غير لو بنكه فيه أسئلة.
- **`exam_passed`** — تهنئة للطالب لما ينجح (≥ passPercent، افتراضي ٥٠٪) — أول محاولة بس.
- **`exam_retry`** — تشجيع للطالب لما يجيب أقل من passPercent، يراجع ويحاول تاني — أول محاولة بس.
- كلها بتفتح **شاشة دروس الفصل** عند الضغط (route جديد `"lessons"` في الموبايل، بيوصّل لكارت الامتحان).
- الكود: `lib/automated-messages.ts` (DEFAULTS) + `routes/gamification-admin.ts` (MESSAGE_ORDER) + **`lib/exam-notifications.ts`** (الجديد: `notifyExamOpened`/`notifyExamResult`) + triggers في `routes/unit-exams.ts` (PUT للفتح، gradeAndSubmit للنتيجة) + `ofouq-mobile/lib/notifications.ts` (route lessons) + `admin/automated-messages-tab.tsx` (تبويب + META). النص/الأيقونة/اللون/التشغيل كله owner-editable زي باقي الرسائل. E2E عدّى: exam_opened للمشتركين بس (مش للي مش مشترك) + exam_passed (١٠٠٪) + exam_retry (١٧٪) + dedupe.

### مؤجّل → اتنقل للـ Backlog (خارج الفيز ٢ المقفولة)
> دي مش شرط للفيز ٢، ومتنقلة للـ Backlog؛ نرجعلها لو احتجناها.
- إحصائيات الأدمن للاختبارات (عدد الحلّالين/متوسط/أصعب سؤال).
- امتحان شامل على مستوى **المادة كلها** (اخترنا الفصل بس دلوقتي).
- الأسئلة القديمة كلها difficulty=medium افتراضياً — الأدمن يحدد الصعوبة عشان المستوى/المتكيّف يشتغلوا بدقة.

### بيانات تجريبية متسابة عمداً (للاختبار)
امتحان فصل **منشور** على وحدة **الأحياء** (unitId=4، ٦ أسئلة نصية) + مستوى للطالب التجريبي (student@demo.com) — عشان الكارت والمستوى يبانوا فوراً عند التجربة.

---

## ✅ الفيز ٣ — التخصيص + «كمّل من حيث وقفت» (بدأت 2026-07-05 · **اتقفلت 2026-07-05**)
الرئيسية بقت **شخصية**، من غير أي تغيير في الداتابيز (استغلال متابعة المشاهدة + الاشتراكات الموجودة).

> **🔒 الفيز ٣ اتقفلت.** كل اللي تحت خلص واتجرّب واتكوميت واترفع على `origin/v2-developments`. **الجاي: الفيز ٤ — باقة المكاسب السريعة** (حفظ الدروس / ملاحظات بتوقيت الفيديو / بحث / لوحة تقدّم / تذكيرات).

### القرارات المتفق عليها مع المالك (AskUserQuestion — جولتين)
- **«كمّل درسك»:** كارت واحد كبير لآخر درس وقف فيه (مش صف).
- **«مقترح ليك»:** مزيج ذكي (الدرس التالي في الفصل + الجديد في مواده + اهتماماته).
- **النطاق:** المواد المشترك فيها **بس** (مفيش تشويق لمواد مقفولة).
- **ترتيب الصفحة:** «كمّل درسك» **أول حاجة فوق** (قبل الترحيب/الستريك).
- **قسم واحد** «مقترح ليك» (المزيج بيغطي الجديد، مفيش قسم منفصل للجديد).
- **بعد الإكمال:** الكارت يوري أي درس تاني لسه ناقص الأول، ولو مفيش → الدرس التالي في الفصل.
- **الطالب الجديد** (مفيش مشاهدات): كارت «ابدأ رحلتك» يوديه لأول درس في أول مادة.

### اللي اتعمل
- **سيرفر:** endpoint واحد `GET /me/home-feed` (في `academic.ts`) بيرجّع `continueLesson` + `startJourney` + `suggestions[]`. كله **scoped على الاشتراكات النشطة**، والتقدّم بالمشاهدة **الحقيقية** (السحب مايحسبش، زي الفيز ٢). المزيج: (١) الناقص، (٢) التالي في فصول بدأها، (٣) الأحدث في مواده — مع dedup وحد أقصى ٨.
- **موبايل:** `lib/homeFeed.ts` (fetch + `openFeedLesson` بيفتح على الثانية اللي وقف عندها) + `components/ContinueLessonCard.tsx` (الكارت الكبير فوق + حالة «ابدأ رحلتك») + `components/SuggestionsSection.tsx` (صف أفقي) + دمجها في `app/(tabs)/index.tsx` (فوق قبل الهيرو + المقترحات بعد شريط التحفيز). i18n عربي/إنجليزي + RTL + refetch عند الرجوع للرئيسية.
- E2E عدّى: طالب عنده درس مفتوح → continue = آخر درس (بيرجع على نفس الثانية) + مقترحات (ناقص + التالي)؛ طالب جديد → «ابدأ رحلتك» + الأحدث (مع dedup). typecheck نضيف في الـ٣ باكدجات.
- **تظبيطات بعد المراجعة (بطلب المالك):** شِلنا الكارت الصغير «الدروس المتاحة الآن» من الكارت الترحيبي (مش عملي) · كارت «كمّل درسك» اتنقل **تحت كارت النقاط/الستريك** مباشرة (بدل فوق خالص) · عنوان «مقترح ليك» بقى ياخد لون الثيم (أبيض في الوضع الداكن).

### مؤجّل للفيز ٤ (مرتبط)
- تذكيرة مذاكرة في «وقت المذاكرة المفضّل» (بند ٤ في الخطة).

---

## 🧹 تظبيطات واجهة إضافية (2026-07-05)
- **شاشة مراجعة إجابات الاختبار (موبايل، `app/quiz.tsx`):** كانت المحاذاة بتتبع **لغة محتوى الاختبار** (بترجع "ar")، فكلمات «Review» و«Question N» كانت تظهر على اليمين حتى في التطبيق الإنجليزي. اتصلحت لتتبع **لغة التطبيق** (`revEn = en`) — التطبيق الإنجليزي بقى LTR بالكامل (Review/Question على أقصى اليسار)، والعربي زي ما هو. كمان عنوان «Result» في الهيدر بقى **متمركز في النص** (`centerTitle`).

---

## ✅ الفيز ٤ — باقة المكاسب السريعة (خلصت 2026-07-05)
٥ مميزات مستقلة، كلها للطلاب + scoped على الاشتراكات. جدولين جداد بس (`lesson_bookmarks` + `lesson_notes`)، الباقي استغلال داتا موجودة.

### القرارات المتفق عليها مع المالك (AskUserQuestion — ٣ دفعات)
- **حفظ ⭐:** النجمة في **شاشة الدرس + كارت القائمة**؛ قائمة «المحفوظة» **جوّه الإعدادات**.
- **ملاحظات 📝:** **تحت الفيديو** في شاشة الدرس؛ الفيديو **واقف وإنت بتكتب** (بيتكتب من الشاشة العادية والفيديو بيشتغل فل-سكرين).
- **بحث 🔎:** **المواد المشترك فيها بس**؛ الشريط **في تبويب الدروس المرئية**.
- **تقدّم 📊:** **جوّه الإعدادات**؛ **كل المقاييس** (نسبة لكل مادة + ساعات المذاكرة + السلسلة/النقاط + متوسط الاختبارات).
- **تذكيرة ⏰:** التوقيت من **«وقت المذاكرة» في الأونبوردنج** (صباح٨/ظهر١٥/مسا١٩/ليل٢١، الافتراضي مسا)؛ **بس الأيام اللي ماذاكرش فيها**؛ **قابلة للتعديل** من «الرسائل المؤتمتة» (تبويب التشجيع).

### اللي اتعمل
- **داتابيز:** `lib/db/src/schema/engagement.ts` (bookmarks + notes) + push.
- **سيرفر:** `routes/engagement.ts` الجديد — `POST/DELETE /lessons/:id/bookmark` + `GET /me/bookmarks` · `GET/POST /lessons/:id/notes` + `PATCH/DELETE /notes/:id` · `GET /search?q=` · `GET /me/progress`. كله student-only + subscription-scoped. تذكيرة المذاكرة: `study_reminder` في `automated-messages.ts` DEFAULTS + `runStudyReminderSweep(hour)` (بيتحوّل bucket→ساعة، ويبعت للي preferred-hour==الساعة الحالية ولسه ماذاكروش) + الـ worker بقى يشغّل التذكيرتين مستقل. مسجّلة في `gamification-admin` MESSAGE_ORDER + `automated-messages-tab` META.
- **موبايل:** `lib/engagement.ts` (fetch helpers + `openLessonRef`) · `components/BookmarkStar.tsx` · `components/LessonRefCard.tsx` · `components/LessonNotes.tsx` · `app/(tabs)/settings/bookmarks.tsx` + `progress.tsx` (+ صفّين في الإعدادات) · `app/(tabs)/videos/search.tsx` (+ شريط بحث في تبويب الدروس) · النجمة + الملاحظات مدموجين في `app/(tabs)/academic/lesson.tsx` (الملاحظة تقفز لتوقيتها عن طريق remount المشغّل على الثانية).
- **E2E عدّى:** حفظ (add/list/remove) · ملاحظات (add/list/edit/delete) · بحث (نتائج + فاضي) · تقدّم (٣ مواد + streak + نقاط + متوسط اختبارات) · study_reminder ظاهر في لوحة الأدمن. typecheck نضيف في الـ٣ باكدجات.
